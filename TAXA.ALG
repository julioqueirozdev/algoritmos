algoritmo "taxa"
var
   valor, taxa: real
inicio
      Escreva("Quanto você pagou no objeto?")
      Leia(valor)
      taxa<- (valor*60)/100
      Escreva("O imposto sobre o produto é US$: ", taxa)
fimalgoritmo